title: Issues

columns:
    left:
        width: 1/3
        sections:
            info:
                type: info
    right:
        width: 2/3
        sections:
            issues:
                type: pages
                label: Parutions
                layout: cards
                template: issue
                flip: true
                text: "{{ model.title }}"
                info: "Nº{{ page.num }} — {{ page.date }}"
                image:
                    cover: true

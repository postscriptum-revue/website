name: field.blocks.image.name
icon: image
preview: image
fields:
    image:
        label: Image
        type: files
        layout: cards
        required: true
    alt:
        label: Texte alternatif
        type: text
        required: true
    caption:
        label: Légende
        type: text
